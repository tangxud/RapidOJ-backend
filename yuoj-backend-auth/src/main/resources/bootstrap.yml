# Tomcat
server:
  port: 8105
  servlet:
    context-path: /api/auth

spring:
  application:
    name: yuoj-backend-auth
  profiles:
    active: dev

knife4j:
  enable: true
  setting:
    language: zh_cn

feign:
  sentinel:
    enabled: true


